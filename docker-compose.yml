version: '3.8'
services:
  db:
    image: postgres:16
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: propchain
    volumes:
      - propchain_pgdata:/var/lib/postgresql/data

volumes:
  propchain_pgdata:
